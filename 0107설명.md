# 뇌종양 임상 의사결정 지원 시스템 (Brain Tumor CDSS)

**작성일**: 2026-01-07

---

## 📋 프로젝트 개요

병원용 뇌종양 진단 및 관리를 위한 Full-Stack 웹 애플리케이션입니다. 의료진이 환자 진료 시 영상 분석, AI 보조 진단, 처방 발행, 검사 오더 등을 통합적으로 관리할 수 있습니다.

---

## 🏗️ 기술 스택

### Frontend (brain_tumor_front)
- **Framework**: React 19.2.0 + TypeScript 5.9.3
- **Build Tool**: Vite 7.2.4
- **UI Library**: Material-UI (MUI) v7.3.6
- **State Management**: React Query (@tanstack/react-query) v5.90.12
- **HTTP Client**: Axios 1.13.2
- **Routing**: React Router DOM v7.11.0
- **Styling**: Emotion (CSS-in-JS)
- **Notifications**: SweetAlert2
- **Icons**: FontAwesome

### Backend (brain_tumor_back)
- **Framework**: Django 5.2.9
- **Language**: Python 3.10+
- **API**: Django REST Framework 3.15.2
- **Real-time**: Django Channels 4.3.2 + Redis 7.1.0
- **Authentication**: SimpleJWT 5.5.1 (JWT 기반)
- **Database**: MySQL 8.0+
- **Server**: Daphne 4.2.1 (ASGI)
- **CORS**: django-cors-headers 4.9.0
- **Standards**: HL7 FHIR R4
- **Task Queue**: Celery

### External Services
- **DICOM Server**: Orthanc (의료 영상 저장)
- **Cache/Session**: Redis 7.1.0
- **FHIR Server**: HAPI FHIR (선택사항)

---

## 📁 프로젝트 구조

```
brain_tumor_dev/
├── brain_tumor_front/          # React Frontend
│   ├── src/
│   │   ├── app/               # 메인 애플리케이션
│   │   ├── router/            # 라우팅 설정
│   │   ├── pages/             # 페이지 컴포넌트
│   │   │   ├── auth/          # 로그인/인증
│   │   │   ├── admin/         # 관리자 기능
│   │   │   ├── dashboard/     # 대시보드
│   │   │   ├── patient/       # 환자 관리
│   │   │   ├── imaging/       # 영상 뷰어
│   │   │   ├── ai/            # AI 분석
│   │   │   ├── ris/           # 방사선 워크리스트
│   │   │   ├── orders/        # 처방 관리
│   │   │   └── lis/           # 검사실
│   │   ├── layout/            # 레이아웃 컴포넌트
│   │   ├── services/          # API 서비스
│   │   ├── socket/            # WebSocket 관리
│   │   ├── hooks/             # Custom Hooks
│   │   ├── types/             # TypeScript 타입
│   │   └── utils/             # 유틸리티
│   ├── package.json
│   └── vite.config.ts
│
├── brain_tumor_back/           # Django Backend
│   ├── config/                # Django 설정
│   │   ├── settings.py
│   │   ├── urls.py
│   │   ├── asgi.py           # WebSocket 설정
│   │   └── routing.py        # WebSocket 라우팅
│   ├── apps/
│   │   ├── accounts/         # 사용자 관리
│   │   ├── authorization/    # 인증/권한
│   │   ├── menus/            # 메뉴 관리
│   │   ├── permissions/      # 권한 관리
│   │   ├── emr/              # 전자의무기록
│   │   ├── ris/              # 영상의학
│   │   ├── lis/              # 검사실
│   │   ├── ocs/              # 처방 시스템
│   │   ├── fhir/             # FHIR 통합
│   │   ├── audit/            # 감사 로그
│   │   └── common/           # 공통 유틸리티
│   ├── requirements.txt
│   └── manage.py
│
├── 초기 더미데이터.sql         # 초기 데이터베이스 데이터
└── 0107설명.md                # 이 문서
```

---

## 🎯 주요 기능

### 1. 인증 및 권한 관리
- **JWT 인증**: Access Token (30분), Refresh Token (1일)
- **로그인 보안**:
  - 로그인 실패 시도 추적
  - 계정 자동 잠금/해제
  - IP 주소 로깅
  - 최초 로그인 시 비밀번호 강제 변경
- **역할 기반 접근 제어 (RBAC)**:
  - 사용자별 권한 할당
  - 메뉴/페이지 동적 표시
  - 실시간 권한 변경 알림 (WebSocket)

### 2. 전자의무기록 (EMR)
- **환자 관리**: 등록, 조회, 수정
- **진료 기록 (Encounters)**:
  - 외래 (Outpatient)
  - 응급 (Emergency)
  - 입원 (Inpatient)
- **처방 관리 (Orders)**:
  - 약물 처방
  - 검사 오더
  - 영상 오더
  - 상태 추적 (대기/완료/취소)
- **진단 코드**: ICD-10/KCD 통합

### 3. 영상의학 시스템 (RIS)
- **DICOM 영상 뷰어**: Orthanc 연동
- **방사선 워크리스트**: 검사 대기 목록
- **영상 검사 오더**: 생성 및 조회
- **DICOM-Web API**: JWT 보안 URL 제공

### 4. 검사실 시스템 (LIS)
- 검사 오더 관리
- 결과 조회

### 5. AI 분석
- AI 기반 영상 분석
- AI 요약 리포트

### 6. 관리자 기능
- **사용자 관리**:
  - 사용자 생성 (ID/비밀번호 자동 생성)
  - 로그인 정보 메일 발송
  - 활성화/비활성화
  - 계정 잠금 해제
- **메뉴/권한 관리**: 메뉴별 권한 설정
- **감사 로그**: 로그인/액션 로그 조회
- **시스템 모니터링**: 시스템 상태 확인

### 7. 실시간 기능 (WebSocket)
- **권한 변경 알림**: `/ws/permissions/`
- **사용자 접속 상태**: `/ws/presence/`
- **개인 권한 업데이트**: `/ws/user-permissions/`
- **Heartbeat 메커니즘**: 30초 TTL

---

## 🔌 API 엔드포인트

### 인증 (Authentication)
```
POST   /api/auth/login/              # 로그인
GET    /api/auth/me/                 # 현재 사용자 정보
POST   /api/auth/change-password/    # 비밀번호 변경
GET    /api/auth/menu/               # 접근 가능 메뉴
POST   /api/token/                   # JWT 토큰 발급
POST   /api/auth/refresh/            # 토큰 갱신
```

### 사용자 관리 (Users)
```
GET    /api/users/                   # 사용자 목록
POST   /api/users/                   # 사용자 생성
GET    /api/users/<id>/              # 사용자 상세
PUT    /api/users/<id>/              # 사용자 수정
DELETE /api/users/<id>/              # 사용자 삭제
POST   /api/users/<id>/toggle-active/ # 활성화 토글
POST   /api/users/<id>/unlock/       # 계정 잠금 해제
```

### 전자의무기록 (EMR)
```
GET    /api/emr/patients/            # 환자 목록
POST   /api/emr/patients/            # 환자 생성
GET    /api/emr/patients/<id>/       # 환자 상세
GET    /api/emr/encounters/          # 진료 기록 목록
GET    /api/emr/orders/              # 처방 목록
POST   /api/emr/openemr/patients/    # OpenEMR 동기화
```

### 영상의학 (RIS)
```
GET    /api/ris/worklist/            # 방사선 워크리스트
GET    /api/ris/studies/             # DICOM 영상 목록
GET    /api/ris/studies/<id>/        # 영상 상세 (DICOM-Web URL 포함)
```

### 기타
```
GET    /api/ocs/                     # 처방 마스터 데이터
GET    /api/lis/                     # 검사실 데이터
GET    /api/fhir/                    # FHIR 리소스
GET    /api/docs/                    # Swagger API 문서
```

---

## 🗄️ 데이터베이스 설정

### 연결 정보
- **호스트**: localhost
- **포트**: 3306
- **데이터베이스**: brain_tumor
- **문자셋**: UTF8MB4 (한글 지원)
- **사용자**: root / root1234 (기본값)

### 주요 테이블
| 테이블명 | 용도 |
|---------|------|
| `accounts_user` | 사용자 계정 |
| `emr_patient_cache` | 환자 정보 캐시 |
| `emr_encounters` | 진료 기록 |
| `emr_orders` | 처방 오더 |
| `emr_order_items` | 처방 항목 |
| `emr_encounter_diagnoses` | 진단 기록 |
| `ris_radiologyorder` | 방사선 오더 |
| `ris_radiologystudy` | DICOM 영상 |
| `ocs_medication_master` | 약품 마스터 |
| `ocs_diagnosis_master` | 진단 마스터 |
| `audit_log` | 감사 로그 |
| `fhir_resourcemap` | FHIR 매핑 |

---

## 🚀 실행 방법

### 사전 요구사항
- Node.js 18+
- Python 3.10+
- MySQL 8.0+
- Redis 7+

### 1. 데이터베이스 설정
```bash
# MySQL 데이터베이스 생성
mysql -u root -p
CREATE DATABASE brain_tumor CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 초기 더미 데이터 삽입
mysql -u root -p brain_tumor < "초기 더미데이터.sql"
```

### 2. Backend 실행
```bash
cd brain_tumor_back

# 가상환경 생성 및 활성화
python -m venv venv
venv\Scripts\activate  # Windows
# source venv/bin/activate  # Mac/Linux

# 패키지 설치
pip install -r requirements.txt

# 환경 변수 설정 (dbconn.env 파일 확인)
# DB_HOST=localhost
# DB_PORT=3306
# DB_NAME=brain_tumor
# DB_USER=root
# DB_PASSWORD=root1234

# 마이그레이션
python manage.py migrate

# Redis 서버 시작 (별도 터미널)
redis-server

# 서버 실행
daphne -b 127.0.0.1 -p 8000 config.asgi:application
```

### 3. Frontend 실행
```bash
cd brain_tumor_front

# 패키지 설치
npm install

# 개발 서버 실행
npm run dev
```

### 4. 접속
- **Frontend**: http://localhost:5173
- **Backend API**: http://localhost:8000
- **API 문서**: http://localhost:8000/api/docs/

### 기본 로그인 정보
더미 데이터에 포함된 계정 정보를 확인하세요.

---

## 🔧 최근 업데이트 (0107)

### 주요 변경사항
1. **관리자 기능 구현**:
   - 사용자 추가 시 ID/비밀번호 자동 생성
   - 로그인 정보 메일 발송 기능
   - 비밀번호 변경 기능

2. **사용자 접속 상태 관리**:
   - WebSocket Consumer 코드 수정
   - Redis 기반 실시간 상태 추적

3. **데이터베이스 통합**:
   - DB 코드 통합 작업 완료

4. **Git 커밋 기록**:
   ```
   29c8de6 0107
   dee3075 Merge branch 'main'
   ac4267c 관리자 기능 - 사용자 추가(아이디, 비번 임의 생성) 구현완료,
           로그인정보 메일발송, 비밀번호변경 기능 구현
   f9bb436 사용자 접속 상태 관리 Consumer 코드 수정
   8ebe11f db 코드 통합 작업 - 완료
   ```

---

## 🏥 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────┐
│          Frontend (React + TypeScript + Vite)           │
├─────────────────────────────────────────────────────────┤
│  Login → Auth → Layout (Sidebar + Header + Content)     │
│  Pages: Dashboard | Patient | Imaging | AI | Orders     │
└────────────────┬────────────────────────────────────────┘
                 │ HTTP (Axios) / WebSocket
                 ▼
┌─────────────────────────────────────────────────────────┐
│       Backend (Django + Channels + DRF + Daphne)        │
├─────────────────────────────────────────────────────────┤
│ ┌─ Authentication (JWT)                                 │
│ ├─ Authorization (RBAC)                                 │
│ ├─ EMR (Patients, Encounters, Orders)                  │
│ ├─ RIS (Radiology, DICOM)                              │
│ ├─ LIS (Laboratory)                                     │
│ ├─ OCS (Order Communication)                           │
│ ├─ FHIR (HL7 Integration)                              │
│ ├─ Audit (Logging)                                      │
│ └─ WebSocket (Real-time notifications)                 │
└────┬─────────┬──────────────┬───────────────────────────┘
     │         │              │
     ▼         ▼              ▼
┌─────────┐ ┌──────┐  ┌──────────────┐
│  MySQL  │ │Redis │  │   Orthanc    │
│Database │ │Cache │  │(DICOM Server)│
└─────────┘ └──────┘  └──────────────┘
```

---

## 📝 개발 가이드

### 코드 스타일
- **Frontend**: ESLint + Prettier (TypeScript)
- **Backend**: PEP 8 (Python)

### 브랜치 전략
- `main`: 메인 브랜치
- 기능 개발 후 메인에 머지

### 환경 설정 파일
- `brain_tumor_front/.env`: Frontend 환경 변수
- `brain_tumor_back/dbconn.env`: DB 연결 정보
- `brain_tumor_back/config/settings.py`: Django 설정

---

## 🔒 보안 고려사항

1. **프로덕션 배포 시 변경 필요**:
   - DB 비밀번호 변경 (root1234 → 강력한 비밀번호)
   - Django SECRET_KEY 변경
   - DEBUG = False 설정
   - ALLOWED_HOSTS 설정
   - CORS_ALLOWED_ORIGINS 제한

2. **인증 보안**:
   - JWT 토큰 만료 시간 조정
   - HTTPS 사용 권장
   - 비밀번호 복잡도 정책

3. **의료 데이터 보안**:
   - 개인정보 암호화
   - 접근 로그 보존
   - HIPAA/GDPR 준수 검토

---

## 📞 문의 및 지원

추가 정보가 필요하거나 문제가 발생한 경우:
- 프로젝트 담당자에게 문의
- Git 이슈 등록
- API 문서 참조: http://localhost:8000/api/docs/

---

## 📚 참고 문서

- [Django Documentation](https://docs.djangoproject.com/)
- [Django Channels](https://channels.readthedocs.io/)
- [React Documentation](https://react.dev/)
- [Material-UI](https://mui.com/)
- [HL7 FHIR](https://www.hl7.org/fhir/)
- [DICOM Standard](https://www.dicomstandard.org/)
- [Orthanc](https://www.orthanc-server.com/)

---

**문서 버전**: 1.0
**최종 업데이트**: 2026-01-07
