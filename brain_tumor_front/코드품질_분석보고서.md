# 🔍 brain_tumor_front 코드 품질 분석 보고서

> 분석일: 2026-01-11

## 프로젝트 개요

| 항목 | 내용 |
|------|------|
| **스택** | React 19 + TypeScript + Vite + MUI 7 |
| **파일 수** | 173개 TypeScript/TSX 파일 |
| **용도** | 뇌종양 진료 관리 시스템 (CDSS) |

---

## 1. 중복 코드 패턴

### 🔴 ListPage 컴포넌트 구조 중복 (심각도: 높음)

| 파일 | 라인 수 |
|------|--------|
| `src/pages/imaging/ImagingListPage.tsx` | 292줄 |
| `src/pages/encounter/EncounterListPage.tsx` | 262줄 |
| `src/pages/patient/PatientListPage.tsx` | 192줄 |

**문제**: 거의 동일한 fetch 패턴이 반복됨

```typescript
// 3개 파일에서 동일한 패턴
const fetchData = async () => {
  setLoading(true);
  try {
    const response = await getData(params);
    if (Array.isArray(response)) { ... }
    else { setData(response.results || []); }
  } catch (error: any) { ... }
  finally { setLoading(false); }
};
```

### 🟡 Modal 컴포넌트 중복 (심각도: 중간)

- `src/pages/imaging/ImagingCreateModal.tsx`
- `src/pages/encounter/EncounterCreateModal.tsx`
- `src/pages/patient/PatientCreateModal.tsx`

환자/의사 목록 로드 로직이 각 모달에서 반복됨

---

## 2. 타입 정의 문제

### 🔴 `any` 타입 과다 사용 (심각도: 높음)

| 파일 | 라인 |
|------|------|
| `src/pages/encounter/EncounterListPage.tsx` | 66 |
| `src/pages/imaging/ImagingListPage.tsx` | 65 |
| `src/pages/imaging/ImagingListTable.tsx` | 104, 117 |
| `src/pages/imaging/ImagingReportPage.tsx` | 52 |
| `src/pages/lab/LabListPage.tsx` | 68 |
| `src/pages/ocs/LISUploadPage.tsx` | 451 |
| `src/pages/ocs/RISUploadPage.tsx` | 471 |

**권장**: `AxiosError` 또는 `unknown` 타입 사용

### 🟡 인라인 Props 정의 (심각도: 중간)

`src/pages/common/SummaryCard.tsx` - interface로 분리 필요

---

## 3. 컴포넌트 구조 문제

### 🔴 거대 컴포넌트 (심각도: 높음)

| 파일 | useState 개수 | 문제점 |
|------|-------------|--------|
| `src/pages/imaging/ImagingListPage.tsx` | 13개 | 검색, 필터, 모달 상태 혼합 |
| `src/pages/encounter/EncounterListPage.tsx` | 11개 | 동일 |
| `src/pages/imaging/ImagingListTable.tsx` | - | 279줄 렌더링 로직 |

**권장 분리**:
- 데이터 로직 → `useImagingList` 훅
- 필터 로직 → `useImagingFilters` 훅
- 모달 상태 → 자식 컴포넌트

### 🟡 인라인 스타일 과다 (심각도: 중간)

`src/pages/imaging/ImagingWorklistPage.tsx:125-180` - CSS 파일로 추출 필요

---

## 4. API 호출 패턴

### ✅ 좋은 점

- `src/services/api.ts` - Axios 인스턴스 잘 구조화
- `src/services/imaging.api.ts` - 일관된 CRUD 패턴
- 401 에러 시 자동 토큰 갱신 처리

### 🔴 응답 타입 처리 불일치 (심각도: 높음)

배열/페이지네이션 객체 두 가지 응답을 컴포넌트마다 개별 처리:

```typescript
// 5개 이상 파일에서 반복
if (Array.isArray(response)) { ... }
else { setData(response.results || []); }
```

**권장**: 서비스 레이어에서 정규화

---

## 5. 에러 처리 패턴

### 🔴 `alert()` 기반 에러 처리 (심각도: 높음)

- **26개 파일**에서 `alert()` 사용
- 사용자 경험 저하, 에러 추적 불가

**주요 파일**:
- `src/pages/auth/LoginPage.tsx:70-114`
- `src/pages/imaging/ImagingListPage.tsx:68`
- `src/pages/encounter/EncounterListPage.tsx:69`

**권장**: Toast/Snackbar 컴포넌트로 교체

### 🟡 에러 상태 관리 불일치 (심각도: 중간)

일부 컴포넌트만 `error` state 사용, 나머지는 console.error만

---

## 6. 상태 관리 패턴

### 🟡 useState 남용 (심각도: 중간)

페이징, 필터, 모달 상태가 분리되지 않고 하나의 컴포넌트에 집중

### ✅ 좋은 사례

- `src/hooks/useOCSList.ts` - 249줄, 체계적 상태 관리
- `src/hooks/useAIInference.ts` - 318줄, 명확한 인터페이스
- `src/pages/auth/AuthProvider.tsx` - 우수한 전역 상태 관리

### 🟡 최적화 부족 (심각도: 낮음)

`useCallback`/`useMemo` 미사용으로 불필요한 재렌더링 발생 가능

---

## 📊 이슈 요약

| 순위 | 이슈 | 파일 수 | 영향도 | 수정 난이도 |
|:----:|------|:------:|:------:|:----------:|
| 1 | ListPage 중복 구조 | 3+ | 높음 | 중간 |
| 2 | `any` 타입 사용 | 9+ | 높음 | 낮음 |
| 3 | 거대 컴포넌트 | 3+ | 높음 | 높음 |
| 4 | `alert()` 에러처리 | 26+ | 중간 | 낮음 |
| 5 | 응답 타입 불일치 | 5+ | 중간 | 중간 |
| 6 | useState 남용 | 10+ | 중간 | 중간 |
| 7 | Modal 중복 구조 | 5+ | 중간 | 중간 |
| 8 | 최적화 부족 | 전체 | 낮음 | 낮음 |

---

## 🎯 개선 권장사항

### 즉시 조치 (High Priority)

1. **커스텀 훅 추출**: `useListData`, `usePagination`, `useFilters`
2. **타입 개선**: `error: any` → `AxiosError` 또는 `unknown`
3. **에러 UI**: `alert()` → Toast/Snackbar 컴포넌트

### 단기 개선 (Medium Priority)

4. **컴포넌트 분할**: ListPage 상태관리/렌더링/필터 분리
5. **API 응답 정규화**: 서비스 레이어에서 통일
6. **성능 최적화**: `useCallback`/`useMemo` 추가

### 장기 개선 (Low Priority)

7. 인라인 스타일 → CSS modules 또는 별도 CSS 파일
8. 주요 훅/유틸리티 단위 테스트 추가

---

## 부록: 프로젝트 구조

```
src/
├── api/                      # API 관련
├── app/                      # 루트 컴포넌트
├── assets/                   # 정적 자원
├── components/               # 공통 컴포넌트
├── hooks/                    # 커스텀 훅
├── layout/                   # 레이아웃 컴포넌트
├── pages/                    # 페이지 컴포넌트
│   ├── admin/               # 관리자 페이지
│   ├── ai/                  # AI 분석
│   ├── ai-inference/        # AI 요청 관리
│   ├── auth/                # 인증
│   ├── dashboard/           # 대시보드
│   ├── encounter/           # 진료 기록
│   ├── imaging/             # 의료 영상
│   ├── lab/                 # 검사실
│   ├── lis/                 # LIS
│   ├── ocs/                 # OCS
│   ├── orders/              # 검사 주문
│   ├── patient/             # 환자 관리
│   └── ris/                 # RIS
├── router/                   # 라우팅 설정
├── services/                 # API 서비스
├── socket/                   # 웹소켓
├── styles/                   # 전역 스타일
├── types/                    # TypeScript 타입
└── utils/                    # 유틸리티
```
