# =============================================================
# Environment Variables Template
# =============================================================
# Copy this file to .env and fill in the values
#   cp .env.example .env
# =============================================================

# =============================================================
# [Versions]
# =============================================================
OPENEMR_VERSION=7.0.2
MARIADB_VERSION=10.6
POSTGRES_VERSION=15
HAPI_FHIR_VERSION=v6.8.3
ORTHANC_VERSION=1.12.4

# =============================================================
# [OpenEMR & MariaDB]
# =============================================================
OE_USER=admin
OE_PASS=admin_password

OE_MYSQL_ROOT_PASS=root_secure_password
OE_MYSQL_USER=openemr
OE_MYSQL_PASS=openemr_password
OE_MYSQL_DB=openemr

# 자동 설치 설정 (첫 설치 후 true로 변경)
OE_SKIP_INSTALLER=false
OE_MANUAL_SETUP=false
OE_SITE=default

# =============================================================
# [HAPI FHIR & PostgreSQL]
# =============================================================
HAPI_DB_USER=hapifhir
HAPI_DB_PASS=hapi_password
HAPI_DB_NAME=hapifhir

# =============================================================
# [Orthanc]
# =============================================================
ORTHANC_DB_USER=orthanc
ORTHANC_DB_PASS=orthanc_password
ORTHANC_DB_NAME=orthanc

# Orthanc API 인증 (modAI에서 DICOM 접근용)
ORTHANC_USER=orthanc
ORTHANC_PASSWORD=orthanc

# =============================================================
# [Django Settings]
# =============================================================
DJANGO_DEBUG=True
DJANGO_SECRET_KEY=your-secret-key
DJANGO_ALLOWED_HOSTS=*

# Django Database (MySQL)
DJANGO_DB_NAME=brain_tumor
DJANGO_DB_USER=root
DJANGO_DB_PASS=root1234
DJANGO_DB_ROOT_PASS=root1234
DJANGO_DB_HOST=django-db
DJANGO_DB_PORT=3306

# Email Settings
EMAIL_HOST_USER=neuronova200@gmail.com
EMAIL_HOST_PASSWORD=fdutbsxrupwcelis

# =============================================================
# [VM Communication] ⚠️ 중요!
# =============================================================
# FastAPI VM에서 메인 VM 서비스 접근용 IP
# ⚠️ 반드시 실제 메인 VM의 IP 주소로 변경하세요!
# 예: 192.168.1.100, 10.0.0.50 등

MAIN_VM_IP=192.168.x.x

# 메인 VM에서 FastAPI VM 접근용 URL
# ⚠️ 반드시 실제 FastAPI VM의 IP 주소로 변경하세요!
FASTAPI_URL=http://192.168.x.x:9000

# =============================================================
# [FastAPI Settings] (별도 VM용)
# =============================================================
FASTAPI_DEBUG=false
APP_ENV=production

# GPU 사용 여부 (Docker 빌드 시)
# true: PyTorch CUDA 버전 설치 (GPU 서버)
# false: PyTorch CPU 버전 설치 (기본값)
USE_GPU=false

# GPU Settings (AI 추론용)
# 사용할 GPU 번호 (0부터 시작, 여러 개면 0,1,2 형식)
CUDA_VISIBLE_DEVICES=0
